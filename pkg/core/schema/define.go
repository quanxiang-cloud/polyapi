package schema

// schema type
const (
	apiSchemaInput  = "input"
	apiSchemaOutput = "output"
)

// schema fields
const (
	schemaTitle      = "title"
	schemaName       = "name"
	schemaProperties = "properties"
	schemaItems      = "items"
	schemaType       = "type"
	schemaSubType    = "subType"
	schemaIn         = "in"
)

// inner doc fields
const (
	docTitle = "title"
	docType  = "type"
	docData  = "data"
	docIn    = "in"
)

// inner doc type
const (
	docTypeObjectVal = "object"
	docTypeArrayVal  = "array"
)
